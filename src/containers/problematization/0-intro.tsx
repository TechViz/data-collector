import React, { FC } from 'react';
import IntroCard from '../../components/layout/intro-card';

const ProblematizationIntro: FC<{}> = ({}) => {
	return (
		<IntroCard
			text="Aqui fazemos um convite para olhar situações e não julgá-las entre certo e errado, pode ou não pode. Se não houvessem barreiras, o que você diria?"
			title="Argumentação"
		/>
	);
};

export default ProblematizationIntro;
