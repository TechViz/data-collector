import React, { FC } from 'react';
import IntroCard from '../../components/layout/intro-card';

const RespectIntro: FC<{}> = ({}) => {
	return (
		<IntroCard
			text="Todos concordamos que é a base de qualquer relacionamento saudável, não é mesmo? Como você sente e observa as relações de trabalho?"
			title="Respeito"
		/>
	);
};

export default RespectIntro;
